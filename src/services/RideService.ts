import * as mongoose from "mongoose";
import * as formidable from "formidable";
import socketObj from "./SocketService";


export class RideService {

    // static async acceptRide(data, callback) {
    //     try {
    //         const {driver_id,booking_id} = data.driver_id;
    //         const bookingDetails={
                
    //         }
           
            
    //     } catch (error) {
            
    //     }
    // }

    static async updateUserLocation(data, callback, socket, io) {
        try {
            
        } catch (error) {
            
        }
    }

    static async sendUserLocation(data, callback, socket, io){
        try {
            
        } catch (error) {
            
        }
    }
}